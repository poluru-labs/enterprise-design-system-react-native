import { Meta } from '@storybook/blocks';

<Meta title="Introduction" />

# Enterprise Design Systems

React Native design system with TypeScript, tokens, icons, and light/dark themes — feature parity with the web components package.

Use the **theme** toolbar to switch light / dark.
